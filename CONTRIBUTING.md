# Contributing · 기여 안내

## English

- Write PR titles and descriptions in English. Prefer English commit messages with `docs:`, `feat:`, `fix:`, or `content:` prefixes.
- Maintain documentation pairs under `docs/en/` and `docs/ko/` together. Shared operational documents may place both languages in the same file.
- Keep user-visible interface strings, explanatory content, and media descriptions available in English and Korean. Preserve Chinese source names and clearly label provisional translations.
- Link evidence for release status, dates, acquisition methods, prices, and name mappings. Record the scope and date of verification.
- Keep forecasts separate from facts. Preserve prior estimates when correcting or superseding them.
- Check rights or applicable reuse terms before storing third-party media. Do not commit secrets or large video collections.
- Keep each PR reviewable. Explain the resulting behavior, checks performed, and any remaining limits. Do not claim tests or deployment that did not happen.
- The repository is private. Its issues are an internal working space, not the eventual public correction channel.
- The app lives in `site/`. Follow the checks and canonical content paths in `docs/en/mvp-status.md`; retain the owner-private audience until public launch is explicitly cleared.

## 한국어

- PR 제목과 본문은 영어로 작성합니다. 커밋 메시지도 `docs:`, `feat:`, `fix:`, `content:` 접두사를 붙인 영어를 기본으로 합니다.
- `docs/en/`와 `docs/ko/`의 대응 문서를 함께 수정합니다. 공통 운영 문서는 한 파일에 두 언어를 모두 넣을 수 있습니다.
- 사용자에게 보이는 UI, 설명 콘텐츠, 미디어 설명은 영어·한국어로 제공합니다. 중국어 원명을 보존하고 임시 번역을 명확히 표시합니다.
- 출시 상태, 날짜, 획득 방식, 가격, 명칭 연결에는 근거를 연결하고 확인 범위와 날짜를 기록합니다.
- 예상과 사실을 분리합니다. 예상이 수정되거나 공식 정보로 대체되어도 이전 판단을 보존합니다.
- 외부 미디어를 저장하기 전에 권한이나 이용 조건을 확인합니다. 비밀키나 대량 영상 파일을 커밋하지 않습니다.
- PR은 검토 가능한 크기로 유지하고 결과, 수행한 확인, 남은 제한을 적습니다. 하지 않은 테스트나 배포를 했다고 적지 않습니다.
- 저장소는 비공개입니다. 저장소 이슈는 내부 작업용이며 향후 공개 제보 창구가 아닙니다.
- 앱은 `site/`에 있습니다. `docs/ko/mvp-status.md`의 검증과 원본 콘텐츠 경로를 따르며 공개 출시가 명시적으로 준비될 때까지 소유자 비공개 범위를 유지합니다.
